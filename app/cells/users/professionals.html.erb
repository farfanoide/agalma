<% @professionals.each do |professional| %>
  <%= link_to image_tag(professional.avatar.url), "#", data: { "reveal-id" => "userModal#{professional.id}" } %>
  <%= render partial: 'professional_modal', locals: {professional: professional} %>
<% end %>
