<% if user_signed_in? %>
  <%= link_to 'Cerrar Sesion', destroy_user_session_path, :method => :delete, class: 'item' %>
<% else %>
  <%= link_to 'Iniciar sesion', "#", data: { "reveal-id" => "login-modal" }, class: 'item' %>
<% end %>
