<% if user_signed_in? %>
  <%= link_to 'Editar Cuenta', edit_user_registration_path, class: 'item' %>
<% else %>
  <%= link_to 'Registrarse', "#", data: { "reveal-id" => "registration-modal" }, class: 'item' %>
<% end %>
