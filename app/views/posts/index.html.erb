<p class="page-title"> <%= t '.noticias' %> </p>

<% @posts.each do |post| %>
  <section class="post-container">
    <div class="post-header">
      <h1 class="post-title"> <%= link_to  post.title, post_path(post) %> </h1>
    </div>
    <div class="post-main">
      <p> <%= raw(post.description)  %> </p>
    </div>
</section>
<% end %>

<%# <%= will_paginate @posts, previous_label: '<<', next_label: '>>'%>

<% if user_signed_in? and policy(current_user).admin? %>
  <%= link_to "crear noticia", new_backend_post_path, class: :'button' %>
<% end %>
