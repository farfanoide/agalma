<%= simple_form_for(@image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :file_name %>
    <%= f.input :name %>
    <%= f.association :galery %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
