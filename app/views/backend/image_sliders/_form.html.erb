<%= simple_form_for([:backend, @image_slider]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :image, label: 'imagen' %>
  </div>

  <br />
  <div class="form-actions">
    <%= f.button :submit, 'agregar imagen', class: :'btn btn-success' %>
  </div>
<% end %>
