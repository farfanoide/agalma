<%= simple_form_for [:backend, @contact] do |c| %>

    <%= c.input :message ,as: :text %>

    <%= c.collection_check_boxes :recipients, @users, :email, :name %>
    <%= c.button :submit, "Enviar Mensaje" %>
<% end %>
