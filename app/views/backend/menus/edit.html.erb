<% if @menu.branch  %>
  <h1> <%= "Editar Menu para la Sucursal #{@menu.branch.name}" %> </h1>
<% else %>
  <h1>Editar Menu</h1>
<% end %>

<%= render 'form' %>

<div class="btn-group">
  <%= link_to 'Show', backend_menu_path(@menu), class: :'btn btn-success' %>
  <%= link_to 'Back', backend_menus_path, class: :'btn btn-danger'%>
</div>
